## Board definitions

Different electronics boards definitions (mostly Arduino and compatible boards), containing dimensions used in models like mounts, boxes etc. 

**Be careful if you're changing anything here, models depending on these may not render correctly!**
